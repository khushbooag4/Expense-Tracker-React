import React,{ useContext } from 'react';
import { GlobalContext } from './context/GlobalState';

export const Transaction1 = (transaction1) => {
    const { transactions }= useContext(GlobalContext);
    
    return (
       <>
          
 </>
    )
}
